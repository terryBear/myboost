/* eslint-disable @typescript-eslint/no-explicit-any */
function Calendar({
  className,
  classNames,
  showOutsideDays = true,
  ...props
}: any) {
  return <p></p>;
}
Calendar.displayName = "Calendar";

export { Calendar };
